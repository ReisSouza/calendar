import React from 'react'

import * as S from './styles'

export type ConfirmStepProps = {}

export const ConfirmStep: React.FC<ConfirmStepProps> = ({
  ...rest
}: ConfirmStepProps) => {
  return <S.ConfirmStepContainer></S.ConfirmStepContainer>
}
