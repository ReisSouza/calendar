import { Text, styled } from '@ionext-ui/react'

export const HeaderContainer = styled('header', {})

export const CalendarTitle = styled(Text, {})

export const CalendarActions = styled('div', {})
